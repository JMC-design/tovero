Some of these examples require an STL viewer.  By default this is 'meshlab', but can be adjusted with:

(setf *mesh-viewer* "<your-STL-viewer-here>")

Others require the 'clive' Common Lisp package to be installed.
